#include <vector>
#include <algorithm>
#include <cmath>

using namespace std;

vector<vector<int>> get_swaps(vector<vector<int>> matrix) {
    // Example: Swap (0,0) with (0,3)
    vector<vector<int>> swaps;
    
    swaps.push_back({0, 0, 0, 3});
    
    return swaps;
}