using Statistics
using Random

function get_swaps(matrix)
    # matrix is a 2D Array
    # Example: Swap the first and second elements of the first row (1-indexed)
    
    swaps = []
    push!(swaps, [1, 1, 1, 2])
    
    return swaps
end